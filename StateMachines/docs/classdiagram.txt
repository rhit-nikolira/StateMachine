@startuml
interface StateMachine{
+boolean accept(List<Character> streamOfChars)
}
class SumOfDigitsEquals3 implements StateMachine {
	
	-List<Transition> transitions;
	+SumOfDigitsEquals3()
	+boolean accept(List<Character> streamOfChars)
	+boolean sumOfDigitsEquals3(int sum, List<Character> streamOfChars)
}
class FindAbcAndPrintThings implements StateMachine {
	-int state = 'A'
	-{static} int FINAL_STATE = 'f'
	-List<Transition> transitions
	+FindAbcAndPrintThings()
	+boolean accept(List<Character> streamOfChars)
	+boolean findAbcAndPrintThings(List<Character> streamOfChars)
	-void runAppropriateSideEffect(char next)
}
@enduml